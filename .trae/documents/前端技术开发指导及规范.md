# 城迹运营后台 - 前端技术开发指导及规范

## 一、文档概述

### 1.1 文档目的
本文档定义"城迹"运营后台前端开发的技术标准、编码规范、目录结构及开发流程，为前端开发团队提供统一的开发指南，确保代码质量和项目可维护性。

### 1.2 适用范围
- 前端开发人员
- 代码审查人员
- 技术负责人

### 1.3 技术栈确认

| 技术组件 | 版本 | 用途 |
|----------|------|------|
| Vue | 3.4+ | 前端框架 |
| Element Plus | 2.5+ | UI组件库 |
| TypeScript | 5.0+ | 类型系统 |
| Vite | 5.0+ | 构建工具 |
| Pinia | 2.1+ | 状态管理 |
| Vue Router | 4.2+ | 路由管理 |
| Axios | 1.6+ | HTTP请求 |
| ECharts | 5.4+ | 数据可视化 |
| Day.js | 1.11+ | 日期处理 |
| ESLint | 8.0+ | 代码检查 |
| Prettier | 3.0+ | 代码格式化 |

---

## 二、项目目录结构

### 2.1 标准目录结构

```
citytrace-admin/
├── .vscode/                    # VS Code配置
│   ├── settings.json           # 编辑器配置
│   └── extensions.json         # 推荐插件
├── public/                     # 静态资源（不经过构建）
│   └── favicon.ico
├── src/                        # 源代码目录
│   ├── api/                    # API接口模块
│   │   ├── modules/            # 按模块划分
│   │   │   ├── user.ts         # 用户相关接口
│   │   │   ├── question.ts     # 题库相关接口
│   │   │   ├── dashboard.ts    # 数据看板接口
│   │   │   ├── content.ts      # 内容管理接口
│   │   │   ├── shop.ts         # 商店管理接口
│   │   │   └── system.ts       # 系统管理接口
│   │   ├── request.ts          # Axios封装
│   │   └── index.ts            # API统一导出
│   │
│   ├── assets/                 # 静态资源（经过构建）
│   │   ├── images/             # 图片资源
│   │   ├── styles/             # 全局样式
│   │   │   ├── variables.scss  # 样式变量
│   │   │   ├── mixins.scss     # 样式混入
│   │   │   ├── reset.scss      # 样式重置
│   │   │   └── index.scss      # 样式入口
│   │   └── fonts/              # 字体资源
│   │
│   ├── components/             # 公共组件
│   │   ├── common/             # 通用组件
│   │   │   ├── Pagination.vue  # 分页组件
│   │   │   ├── SearchForm.vue  # 搜索表单
│   │   │   ├── TableList.vue   # 表格列表
│   │   │   └── UploadImage.vue # 图片上传
│   │   ├── business/           # 业务组件
│   │   │   ├── QuestionCard.vue    # 题目卡片
│   │   │   ├── UserDetail.vue      # 用户详情
│   │   │   └── DataChart.vue       # 数据图表
│   │   └── layout/             # 布局组件
│   │       ├── Sidebar.vue     # 侧边栏
│   │       ├── Header.vue      # 顶部栏
│   │       └── MainContent.vue # 主内容区
│   │
│   ├── composables/            # 组合式函数（Hooks）
│   │   ├── useTable.ts         # 表格相关逻辑
│   │   ├── useForm.ts          # 表单相关逻辑
│   │   ├── usePermission.ts    # 权限相关逻辑
│   │   └── useExport.ts        # 导出相关逻辑
│   │
│   ├── directives/             # 自定义指令
│   │   ├── permission.ts       # 权限指令
│   │   ├── loading.ts          # 加载指令
│   │   └── index.ts            # 指令统一导出
│   │
│   ├── enums/                  # 枚举定义
│   │   ├── question.ts         # 题目相关枚举
│   │   ├── user.ts             # 用户相关枚举
│   │   └── common.ts           # 通用枚举
│   │
│   ├── layouts/                # 布局组件
│   │   ├── DefaultLayout.vue   # 默认布局
│   │   └── BlankLayout.vue     # 空白布局
│   │
│   ├── router/                 # 路由配置
│   │   ├── modules/            # 路由模块
│   │   │   ├── dashboard.ts    # 数据看板路由
│   │   │   ├── user.ts         # 用户管理路由
│   │   │   ├── question.ts     # 题库管理路由
│   │   │   ├── content.ts      # 内容管理路由
│   │   │   ├── shop.ts         # 商店管理路由
│   │   │   └── system.ts       # 系统管理路由
│   │   ├── guards.ts           # 路由守卫
│   │   └── index.ts            # 路由入口
│   │
│   ├── stores/                 # 状态管理（Pinia）
│   │   ├── modules/            # 状态模块
│   │   │   ├── user.ts         # 用户状态
│   │   │   ├── permission.ts   # 权限状态
│   │   │   └── app.ts          # 应用状态
│   │   └── index.ts            # 状态入口
│   │
│   ├── types/                  # TypeScript类型定义
│   │   ├── api.d.ts            # API响应类型
│   │   ├── user.d.ts           # 用户相关类型
│   │   ├── question.d.ts       # 题目相关类型
│   │   └── common.d.ts         # 通用类型
│   │
│   ├── utils/                  # 工具函数
│   │   ├── auth.ts             # 认证相关
│   │   ├── storage.ts          # 存储相关
│   │   ├── format.ts           # 格式化工具
│   │   ├── validate.ts         # 验证工具
│   │   └── download.ts         # 下载工具
│   │
│   ├── views/                  # 页面视图
│   │   ├── dashboard/          # 数据看板
│   │   │   └── index.vue
│   │   ├── user/               # 用户管理
│   │   │   ├── list/index.vue      # 用户列表
│   │   │   ├── detail/index.vue    # 用户详情
│   │   │   └── feedback/index.vue  # 用户反馈
│   │   ├── question/           # 题库管理
│   │   │   ├── list/index.vue      # 题目列表
│   │   │   ├── edit/index.vue      # 题目编辑
│   │   │   ├── audit/index.vue     # 题目审核
│   │   │   └── import/index.vue    # 批量导入
│   │   ├── level/              # 关卡管理
│   │   │   ├── list/index.vue      # 关卡列表
│   │   │   ├── rule/index.vue      # 层级规则
│   │   │   └── region/index.vue    # 区域主题
│   │   ├── content/            # 内容管理
│   │   │   ├── recommend/index.vue # 每日推荐
│   │   │   ├── city/index.vue      # 城市数据
│   │   │   ├── country/index.vue   # 国家数据
│   │   │   └── flag/index.vue      # 国旗图片库
│   │   ├── shop/               # 商店管理
│   │   │   ├── goods/index.vue     # 商品配置
│   │   │   └── price/index.vue     # 价格管理
│   │   ├── rank/               # 排行榜管理
│   │   │   ├── list/index.vue      # 排行数据
│   │   │   └── anomaly/index.vue   # 异常检测
│   │   ├── system/             # 系统管理
│   │   │   ├── admin/index.vue     # 管理员账号
│   │   │   ├── role/index.vue      # 角色权限
│   │   │   └── log/index.vue       # 操作日志
│   │   ├── login/index.vue     # 登录页
│   │   └── error/              # 错误页面
│   │       ├── 404.vue
│   │       └── 403.vue
│   │
│   ├── App.vue                 # 根组件
│   ├── main.ts                 # 入口文件
│   └── env.d.ts                # 环境变量类型
│
├── .env                        # 环境变量
├── .env.development            # 开发环境变量
├── .env.production             # 生产环境变量
├── .eslintrc.cjs               # ESLint配置
├── .prettierrc                 # Prettier配置
├── .gitignore                  # Git忽略文件
├── index.html                  # HTML入口
├── package.json                # 项目配置
├── tsconfig.json               # TypeScript配置
├── vite.config.ts              # Vite配置
└── README.md                   # 项目说明
```

### 2.2 目录职责说明

| 目录 | 职责 | 说明 |
|------|------|------|
| api | API接口管理 | 按模块划分，统一管理接口请求 |
| assets | 静态资源 | 经过构建处理的资源文件 |
| components | 组件库 | 公共组件和业务组件 |
| composables | 组合式函数 | 可复用的逻辑函数 |
| directives | 自定义指令 | Vue自定义指令 |
| enums | 枚举定义 | 常量和枚举值 |
| layouts | 布局组件 | 页面布局模板 |
| router | 路由配置 | 路由定义和守卫 |
| stores | 状态管理 | Pinia状态管理 |
| types | 类型定义 | TypeScript类型声明 |
| utils | 工具函数 | 通用工具方法 |
| views | 页面视图 | 业务页面组件 |

---

## 三、命名规范

### 3.1 文件命名

| 类型 | 命名规范 | 示例 |
|------|----------|------|
| Vue组件 | PascalCase | `UserList.vue`, `QuestionCard.vue` |
| TypeScript文件 | camelCase | `useTable.ts`, `formatDate.ts` |
| 类型定义文件 | camelCase + .d.ts | `user.d.ts`, `api.d.ts` |
| 样式文件 | kebab-case | `variables.scss`, `reset.scss` |
| 图片资源 | kebab-case | `logo-primary.png`, `icon-user.svg` |
| 目录名 | kebab-case | `user-list/`, `question-edit/` |

### 3.2 变量命名

| 类型 | 命名规范 | 示例 |
|------|----------|------|
| 普通变量 | camelCase | `userName`, `totalCount` |
| 常量 | UPPER_SNAKE_CASE | `MAX_COUNT`, `API_BASE_URL` |
| 私有变量 | _camelCase | `_privateData`, `_internalState` |
| 布尔值 | is/has/can + camelCase | `isLoading`, `hasPermission`, `canEdit` |
| 数组 | 复数形式或List后缀 | `users`, `questionList` |
| 对象 | 单数形式 | `user`, `question` |
| 函数 | 动词开头 | `getUserList`, `handleSubmit`, `formatDate` |
| 异步函数 | async前缀或动词 | `asyncFetchData`, `loadUserData` |
| 事件处理 | handle + 事件名 | `handleClick`, `handleSubmit` |
| 回调函数 | on + 事件名 | `onSuccess`, `onError` |

### 3.3 组件命名

| 类型 | 命名规范 | 示例 |
|------|----------|------|
| 页面组件 | 功能名 + Page/View | `UserListPage`, `DashboardView` |
| 业务组件 | 功能描述 | `QuestionCard`, `UserDetail` |
| 通用组件 | 功能描述 | `Pagination`, `SearchForm` |
| 布局组件 | Layout后缀 | `DefaultLayout`, `BlankLayout` |
| 高阶组件 | with前缀 | `withAuth`, `withLoading` |

### 3.4 Props命名

| 类型 | 命名规范 | 示例 |
|------|----------|------|
| 普通属性 | camelCase | `userName`, `totalCount` |
| 布尔属性 | is/has/can前缀 | `isVisible`, `hasBorder`, `canEdit` |
| 事件属性 | on前缀 | `onClick`, `onChange` |
| 对象属性 | 具体名称 | `userInfo`, `config` |

### 3.5 CSS命名（BEM规范）

```scss
.block {}
.block__element {}
.block--modifier {}

.question-card {}
.question-card__title {}
.question-card__content {}
.question-card--featured {}
.question-card__title--highlighted {}
```

### 3.6 枚举命名

```typescript
enum QuestionType {
  CITY_TO_PROVINCE = 1,
  CITY_TO_COUNTRY = 2,
  ANCIENT_TO_MODERN = 3,
  FLAG_TO_COUNTRY = 4
}

enum QuestionStatus {
  ENABLED = 1,
  DISABLED = 0,
  PENDING = 2
}
```

---

## 四、编码规范

### 4.1 Vue组件规范

#### 4.1.1 组件结构

```vue
<template>
  <div class="component-name">
  </div>
</template>

<script setup lang="ts">
import { ref, computed, onMounted } from 'vue'
import type { PropType } from 'vue'

interface Props {
  title: string
  count?: number
}

const props = withDefaults(defineProps<Props>(), {
  count: 0
})

const emit = defineEmits<{
  (e: 'update', value: number): void
  (e: 'submit'): void
}>()

const localData = ref<string>('')

const computedValue = computed(() => {
  return props.count * 2
})

const handleClick = () => {
  emit('update', computedValue.value)
}

onMounted(() => {
})
</script>

<style scoped lang="scss">
.component-name {
}
</style>
```

#### 4.1.2 组件编写顺序

```vue
<script setup lang="ts">
// 1. 导入语句
import { ref, computed, watch, onMounted } from 'vue'
import { useRouter } from 'vue-router'
import { useUserStore } from '@/stores/modules/user'
import type { UserInfo } from '@/types/user'

// 2. Props定义
interface Props {
  userId: string
}
const props = defineProps<Props>()

// 3. Emits定义
const emit = defineEmits<{
  (e: 'update', value: UserInfo): void
}>()

// 4. 响应式数据
const loading = ref(false)
const userInfo = ref<UserInfo | null>(null)

// 5. 计算属性
const displayName = computed(() => {
  return userInfo.value?.nickname || '未知用户'
})

// 6. 监听器
watch(() => props.userId, (newId) => {
  fetchUserInfo(newId)
})

// 7. 方法
const fetchUserInfo = async (id: string) => {
  loading.value = true
  try {
    // ...
  } finally {
    loading.value = false
  }
}

const handleSubmit = () => {
  emit('update', userInfo.value!)
}

// 8. 生命周期
onMounted(() => {
  fetchUserInfo(props.userId)
})
</script>
```

### 4.2 TypeScript规范

#### 4.2.1 类型定义

```typescript
interface UserInfo {
  id: string
  nickname: string
  avatar: string
  province: string
  totalStars: number
  levelCount: number
  createdAt: string
}

interface ApiResponse<T> {
  code: number
  message: string
  data: T
}

interface PageParams {
  page: number
  pageSize: number
}

interface PageResult<T> {
  list: T[]
  total: number
  page: number
  pageSize: number
}
```

#### 4.2.2 泛型使用

```typescript
function request<T>(url: string, params?: Record<string, unknown>): Promise<ApiResponse<T>> {
  return axios.get(url, { params })
}

async function getUserList(params: PageParams): Promise<PageResult<UserInfo>> {
  const res = await request<PageResult<UserInfo>>('/api/admin/user/list', params)
  return res.data
}
```

#### 4.2.3 类型断言

```typescript
const value = data as UserInfo

const element = document.querySelector('.container') as HTMLDivElement

const keys = Object.keys(obj) as Array<keyof typeof obj>
```

### 4.3 API请求规范

#### 4.3.1 请求封装

```typescript
import axios, { type AxiosRequestConfig, type AxiosResponse } from 'axios'
import { getToken, removeToken } from '@/utils/auth'
import { ElMessage } from 'element-plus'

const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 30000
})

request.interceptors.request.use(
  (config) => {
    const token = getToken()
    if (token) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  },
  (error) => {
    return Promise.reject(error)
  }
)

request.interceptors.response.use(
  (response: AxiosResponse) => {
    const { code, message, data } = response.data
    if (code === 0) {
      return data
    }
    ElMessage.error(message || '请求失败')
    return Promise.reject(new Error(message))
  },
  (error) => {
    if (error.response?.status === 401) {
      removeToken()
      window.location.href = '/login'
    }
    ElMessage.error(error.message || '网络错误')
    return Promise.reject(error)
  }
)

export default request
```

#### 4.3.2 API模块定义

```typescript
import request from '../request'
import type { UserInfo, PageParams, PageResult } from '@/types'

export const userApi = {
  getList(params: PageParams & { nickname?: string; province?: string }) {
    return request.get<PageResult<UserInfo>>('/admin/user/list', { params })
  },

  getDetail(id: string) {
    return request.get<UserInfo>(`/admin/user/${id}`)
  },

  getStatistics(id: string) {
    return request.get<{
      totalAnswerCount: number
      correctRate: number
      maxStarLevel: number
    }>(`/admin/user/${id}/statistics`)
  }
}
```

### 4.4 状态管理规范

#### 4.4.1 Store定义

```typescript
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import type { UserInfo } from '@/types'

export const useUserStore = defineStore('user', () => {
  const token = ref<string>('')
  const userInfo = ref<UserInfo | null>(null)

  const isLoggedIn = computed(() => !!token.value)
  const userName = computed(() => userInfo.value?.nickname || '')

  const setToken = (newToken: string) => {
    token.value = newToken
  }

  const setUserInfo = (info: UserInfo) => {
    userInfo.value = info
  }

  const logout = () => {
    token.value = ''
    userInfo.value = null
  }

  return {
    token,
    userInfo,
    isLoggedIn,
    userName,
    setToken,
    setUserInfo,
    logout
  }
}, {
  persist: true
})
```

### 4.5 路由规范

#### 4.5.1 路由定义

```typescript
import type { RouteRecordRaw } from 'vue-router'

export const userRoutes: RouteRecordRaw[] = [
  {
    path: '/user',
    component: () => import('@/layouts/DefaultLayout.vue'),
    meta: { title: '用户管理', icon: 'User' },
    children: [
      {
        path: 'list',
        name: 'UserList',
        component: () => import('@/views/user/list/index.vue'),
        meta: { title: '用户列表', keepAlive: true }
      },
      {
        path: 'detail/:id',
        name: 'UserDetail',
        component: () => import('@/views/user/detail/index.vue'),
        meta: { title: '用户详情', hidden: true }
      },
      {
        path: 'feedback',
        name: 'UserFeedback',
        component: () => import('@/views/user/feedback/index.vue'),
        meta: { title: '用户反馈' }
      }
    ]
  }
]
```

#### 4.5.2 路由守卫

```typescript
import type { Router } from 'vue-router'
import { useUserStore } from '@/stores/modules/user'
import { getToken } from '@/utils/auth'
import { whiteList } from '@/config'

export function setupRouterGuard(router: Router) {
  router.beforeEach((to, from, next) => {
    const token = getToken()

    if (token) {
      if (to.path === '/login') {
        next({ path: '/' })
      } else {
        const userStore = useUserStore()
        if (userStore.userInfo) {
          next()
        } else {
          userStore.getUserInfo().then(() => {
            next()
          }).catch(() => {
            next({ path: '/login', query: { redirect: to.fullPath } })
          })
        }
      }
    } else {
      if (whiteList.includes(to.path)) {
        next()
      } else {
        next({ path: '/login', query: { redirect: to.fullPath } })
      }
    }
  })
}
```

### 4.6 样式规范

#### 4.6.1 样式变量

```scss
$primary-color: #1890ff;
$success-color: #52c41a;
$warning-color: #faad14;
$error-color: #f5222d;

$text-color: #262626;
$text-color-secondary: #8c8c8c;

$border-color: #e8e8e8;
$background-color: #f0f2f5;

$font-size-base: 14px;
$font-size-sm: 12px;
$font-size-lg: 16px;

$border-radius-base: 4px;
$border-radius-sm: 2px;
$border-radius-lg: 8px;

$spacing-xs: 4px;
$spacing-sm: 8px;
$spacing-md: 16px;
$spacing-lg: 24px;
```

#### 4.6.2 组件样式

```vue
<style scoped lang="scss">
.user-list {
  padding: $spacing-lg;
  background: #fff;
  border-radius: $border-radius-base;

  &__header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: $spacing-md;
  }

  &__title {
    font-size: $font-size-lg;
    font-weight: 500;
    color: $text-color;
  }

  &__search {
    display: flex;
    gap: $spacing-sm;
  }

  &__table {
    margin-top: $spacing-md;
  }

  &__pagination {
    display: flex;
    justify-content: flex-end;
    margin-top: $spacing-md;
  }
}
</style>
```

---

## 五、代码注释规范

### 5.1 文件头注释

```typescript
/**
 * @description 用户管理API接口
 * @author 开发者姓名
 * @date 2026-03-23
 */
```

### 5.2 函数注释

```typescript
/**
 * 获取用户列表数据
 * @param params 查询参数
 * @param params.page 页码
 * @param params.pageSize 每页数量
 * @param params.nickname 用户昵称（可选）
 * @returns 用户列表数据
 */
export async function getUserList(params: PageParams & { nickname?: string }): Promise<PageResult<UserInfo>> {
  // ...
}
```

### 5.3 复杂逻辑注释

```typescript
const calculateStarLevel = (correctRate: number, avgTime: number): number => {
  let baseLevel = 5

  if (correctRate < 0.2) {
    baseLevel = 0.5
  } else if (correctRate < 0.3) {
    baseLevel = 1
  } else if (correctRate < 0.4) {
    baseLevel = 1.5
  }

  // 时间加成：平均答题时间<=5秒额外获得1星，5-10秒获得0.5星
  let timeBonus = 0
  if (avgTime <= 5) {
    timeBonus = 1
  } else if (avgTime <= 10) {
    timeBonus = 0.5
  }

  return Math.min(baseLevel + timeBonus, 6)
}
```

### 5.4 TODO注释

```typescript
// TODO: 后续需要添加缓存机制，减少重复请求
// FIXME: 当数据量超过1000条时，分页组件显示异常
// HACK: 临时解决方案，等待后端接口优化后移除
```

---

## 六、版本控制规范

### 6.1 分支管理

```
main (生产分支)
  │
  ├── develop (开发分支)
  │     │
  │     ├── feature/user-management (功能分支)
  │     ├── feature/question-management (功能分支)
  │     └── feature/dashboard (功能分支)
  │
  ├── release/v1.0.0 (发布分支)
  │
  └── hotfix/fix-login-bug (热修复分支)
```

### 6.2 分支命名规范

| 分支类型 | 命名规范 | 示例 |
|----------|----------|------|
| 功能分支 | feature/功能描述 | `feature/user-management` |
| 修复分支 | fix/问题描述 | `fix/login-error` |
| 发布分支 | release/版本号 | `release/v1.0.0` |
| 热修复分支 | hotfix/问题描述 | `hotfix/fix-login-bug` |

### 6.3 Commit规范

#### 6.3.1 Commit格式

```
<type>(<scope>): <subject>

<body>

<footer>
```

#### 6.3.2 Type类型

| Type | 说明 | 示例 |
|------|------|------|
| feat | 新功能 | `feat(user): 新增用户列表导出功能` |
| fix | 修复Bug | `fix(login): 修复登录验证码失效问题` |
| docs | 文档更新 | `docs(readme): 更新项目说明文档` |
| style | 代码格式 | `style: 格式化代码缩进` |
| refactor | 重构 | `refactor(api): 重构用户接口请求逻辑` |
| perf | 性能优化 | `perf(table): 优化大列表渲染性能` |
| test | 测试 | `test(user): 添加用户模块单元测试` |
| chore | 构建/工具 | `chore: 更新依赖版本` |
| revert | 回滚 | `revert: 回滚用户模块修改` |

#### 6.3.3 Scope范围

| Scope | 说明 |
|-------|------|
| user | 用户管理模块 |
| question | 题库管理模块 |
| dashboard | 数据看板模块 |
| content | 内容管理模块 |
| shop | 商店管理模块 |
| system | 系统管理模块 |
| api | API接口 |
| utils | 工具函数 |
| styles | 样式文件 |

### 6.4 Commit示例

```bash
feat(user): 新增用户列表导出功能

- 新增导出按钮和导出逻辑
- 支持按筛选条件导出
- 导出格式为Excel

Closes #123
```

```bash
fix(login): 修复登录验证码失效问题

登录页面验证码在刷新后失效的问题已修复

Fixes #456
```

---

## 七、代码审查流程

### 7.1 审查流程

```
开发完成 → 自测通过 → 提交PR → 代码审查 → 审查通过 → 合并代码
                ↓                          ↓
            修复问题 ←──────────────── 审查不通过
```

### 7.2 审查要点

| 审查维度 | 审查内容 | 重要性 |
|----------|----------|--------|
| 代码规范 | 命名、格式、注释是否符合规范 | 高 |
| 功能正确 | 功能是否按需求实现 | 高 |
| 性能影响 | 是否存在性能问题 | 高 |
| 安全风险 | 是否存在安全漏洞 | 高 |
| 代码复用 | 是否存在重复代码 | 中 |
| 可维护性 | 代码结构是否清晰 | 中 |
| 测试覆盖 | 是否有足够的测试 | 中 |

### 7.3 审查清单

- [ ] 代码是否符合命名规范
- [ ] 是否有必要的注释
- [ ] 是否有TypeScript类型定义
- [ ] 是否有错误处理
- [ ] 是否有性能问题
- [ ] 是否有安全风险
- [ ] 是否有重复代码
- [ ] 是否有单元测试

### 7.4 PR模板

```markdown
## 变更说明
<!-- 简要描述本次变更的内容 -->

## 变更类型
- [ ] 新功能 (feat)
- [ ] 修复Bug (fix)
- [ ] 重构 (refactor)
- [ ] 文档更新 (docs)
- [ ] 其他

## 关联Issue
<!-- 关联的Issue编号，如 Closes #123 -->

## 测试说明
<!-- 描述如何测试本次变更 -->

## 截图
<!-- 如有UI变更，请附上截图 -->

## 检查清单
- [ ] 代码符合编码规范
- [ ] 已添加必要注释
- [ ] 已完成自测
- [ ] 已更新相关文档
```

---

## 八、开发流程规范

### 8.1 开发流程

```
需求分析 → 技术方案 → 编码实现 → 单元测试 → 代码审查 → 集成测试 → 发布上线
```

### 8.2 开发规范

| 阶段 | 规范要求 |
|------|----------|
| 需求分析 | 充分理解需求，明确技术边界 |
| 技术方案 | 输出技术方案文档，评审通过后开发 |
| 编码实现 | 遵循编码规范，编写可维护代码 |
| 单元测试 | 核心功能需有单元测试 |
| 代码审查 | 提交PR，通过审查后合并 |
| 集成测试 | 完成功能测试和集成测试 |
| 发布上线 | 遵循发布流程，做好回滚准备 |

### 8.3 开发环境配置

#### 8.3.1 VS Code插件推荐

```json
{
  "recommendations": [
    "Vue.volar",
    "Vue.vscode-typescript-vue-plugin",
    "dbaeumer.vscode-eslint",
    "esbenp.prettier-vscode",
    "stylelint.vscode-stylelint",
    "mrmlnc.vscode-scss",
    "yzhang.markdown-all-in-one"
  ]
}
```

#### 8.3.2 VS Code配置

```json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": "explicit"
  },
  "typescript.tsdk": "node_modules/typescript/lib",
  "vue.inlayHints.missingProps": true,
  "vue.inlayHints.inlineHandlerLeading": true
}
```

---

## 九、性能优化规范

### 9.1 前端性能指标

| 指标 | 目标值 |
|------|--------|
| 首屏加载时间 | < 3秒 |
| 白屏时间 | < 1秒 |
| 首次内容绘制(FCP) | < 1.8秒 |
| 最大内容绘制(LCP) | < 2.5秒 |
| 累积布局偏移(CLS) | < 0.1 |

### 9.2 优化策略

| 优化项 | 实现方式 |
|--------|----------|
| 代码分割 | 路由级别懒加载 |
| 资源压缩 | Vite构建压缩 |
| 图片优化 | 懒加载 + WebP格式 |
| 缓存策略 | 静态资源CDN + 强缓存 |
| 请求优化 | 接口合并 + 数据缓存 |
| 渲染优化 | 虚拟列表 + 防抖节流 |

### 9.3 代码示例

#### 9.3.1 路由懒加载

```typescript
const routes = [
  {
    path: '/user/list',
    component: () => import('@/views/user/list/index.vue')
  }
]
```

#### 9.3.2 虚拟列表

```vue
<template>
  <el-table-v2
    :columns="columns"
    :data="data"
    :width="800"
    :height="400"
    :row-height="50"
    fixed
  />
</template>
```

#### 9.3.3 防抖节流

```typescript
import { debounce, throttle } from 'lodash-es'

const handleSearch = debounce((keyword: string) => {
  // 搜索逻辑
}, 300)

const handleScroll = throttle(() => {
  // 滚动逻辑
}, 100)
```

---

## 十、安全规范

### 10.1 前端安全要点

| 安全项 | 防护措施 |
|--------|----------|
| XSS攻击 | 输入过滤 + 输出编码 |
| CSRF攻击 | Token验证 |
| 敏感信息 | 不在前端存储敏感信息 |
| 请求安全 | HTTPS + 请求签名 |
| 权限控制 | 路由守卫 + 按钮权限 |

### 10.2 代码示例

#### 10.2.1 XSS防护

```typescript
import { escape } from 'lodash-es'

const sanitizeInput = (input: string): string => {
  return escape(input)
}
```

#### 10.2.2 权限控制

```vue
<template>
  <el-button v-permission="'user:edit'" type="primary">编辑</el-button>
</template>

<script setup lang="ts">
import { usePermissionStore } from '@/stores/modules/permission'

const permissionStore = usePermissionStore()

const vPermission = {
  mounted(el: HTMLElement, binding: DirectiveBinding) {
    const { value } = binding
    if (!permissionStore.hasPermission(value)) {
      el.parentNode?.removeChild(el)
    }
  }
}
</script>
```

---

## 附录

### A. ESLint配置

```javascript
module.exports = {
  root: true,
  env: {
    browser: true,
    es2021: true,
    node: true
  },
  extends: [
    'eslint:recommended',
    'plugin:vue/vue3-recommended',
    'plugin:@typescript-eslint/recommended',
    'prettier'
  ],
  parser: 'vue-eslint-parser',
  parserOptions: {
    ecmaVersion: 'latest',
    parser: '@typescript-eslint/parser',
    sourceType: 'module'
  },
  plugins: ['vue', '@typescript-eslint'],
  rules: {
    'vue/multi-word-component-names': 'off',
    'vue/no-v-html': 'warn',
    '@typescript-eslint/no-explicit-any': 'warn',
    '@typescript-eslint/no-unused-vars': 'error',
    'no-console': process.env.NODE_ENV === 'production' ? 'warn' : 'off',
    'no-debugger': process.env.NODE_ENV === 'production' ? 'warn' : 'off'
  }
}
```

### B. Prettier配置

```json
{
  "semi": false,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "none",
  "printWidth": 100,
  "bracketSpacing": true,
  "arrowParens": "avoid",
  "endOfLine": "auto",
  "vueIndentScriptAndStyle": false
}
```

### C. 版本历史

| 版本 | 日期 | 修改内容 | 修改人 |
|------|------|----------|--------|
| v1.0 | 2026-03-23 | 初始版本 | 技术经理 |

---

**文档状态**：待评审  
**下一步行动**：组织团队培训，确保规范落地执行
